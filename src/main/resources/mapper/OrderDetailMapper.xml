<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jerry.restaurant.mapper.OrderDetailMapper">
    <select id="getOrderDetailsByOrderId" parameterType="int" resultType="com.example.jerry.restaurant.pojo.OrderDetail">
        SELECT * FROM order_detail WHERE order_id = #{orderId}
    </select>
    <select id="getOrderDetailsWithDishInfo" parameterType="int" resultType="com.example.jerry.restaurant.pojo.OrderDetail">
        SELECT od.*, m.dish_name, m.price FROM order_detail od JOIN menu m ON od.dish_id = m.dish_id WHERE od.order_id = #{orderId}
    </select>
    <insert id="addOrderDetail" parameterType="com.example.jerry.restaurant.pojo.OrderDetail" useGeneratedKeys="true" keyProperty="detailId">
        INSERT INTO order_detail (order_id, dish_id, quantity) VALUES (#{orderId}, #{dishId}, #{quantity})
    </insert>
    <update id="updateOrderDetailQuantity">
        UPDATE order_detail SET quantity = #{quantity} WHERE detail_id = #{detailId}
    </update>
    <delete id="deleteOrderDetail" parameterType="int">
        DELETE FROM order_detail WHERE detail_id = #{detailId}
    </delete>
    <delete id="deleteOrderDetailByOrderAndDish">
        DELETE FROM order_detail WHERE order_id = #{orderId} AND dish_id = #{dishId}
    </delete>
    <select id="checkOrderDetailExists" resultType="int">
        SELECT COUNT(*) FROM order_detail WHERE order_id = #{orderId} AND dish_id = #{dishId}
    </select>
    <select id="getAllOrderDetails" resultType="com.example.jerry.restaurant.pojo.OrderDetail">
        SELECT * FROM order_detail
    </select>
</mapper> 