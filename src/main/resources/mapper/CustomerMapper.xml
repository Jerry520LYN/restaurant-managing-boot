<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jerry.restaurant.mapper.CustomerMapper">
    <select id="getCustomerByPhone" parameterType="string" resultType="com.example.jerry.restaurant.pojo.Customer">
        select customer_id, name, CONCAT(LEFT(phone, 3), '****', RIGHT(phone, 4)) as phone, create_time  from customer where phone=#{phone}
    </select>
    <select id="getCustomerById" parameterType="int" resultType="com.example.jerry.restaurant.pojo.Customer">
        select customer_id, name, CONCAT(LEFT(phone, 3), '****', RIGHT(phone, 4)) as phone, create_time from customer where customer_id= #{id}
    </select>
    <select id="getCustomerWithMaskedPhoneById" parameterType="int" resultType="com.example.jerry.restaurant.pojo.Customer">
        select customer_id, name, CONCAT(LEFT(phone, 3), '****', RIGHT(phone, 4)) as phone, create_time from customer where customer_id= #{id}
    </select>
    <insert id="addCustomer" parameterType="com.example.jerry.restaurant.pojo.Customer">
        insert into customer (customer_id,name,phone,create_time) values (#{customer_id},#{name},#{phone},now())
    </insert>
    <update id="updateCustomer">
        update customer set customer_id = #{customer.customer_id}, name = #{customer.name}, phone = #{customer.phone} where phone = #{oldphone}
    </update>
    <delete id="deleteCustomer" parameterType="string">
        delete from customer where phone =#{phone}
    </delete>
    <select id="getAllCustomers" resultType="com.example.jerry.restaurant.pojo.Customer">
        select customer_id, name, CONCAT(LEFT(phone, 3), '****', RIGHT(phone, 4)) as phone, create_time from customer
    </select>
</mapper> 